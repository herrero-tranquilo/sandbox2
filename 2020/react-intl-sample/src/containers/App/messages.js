import { defineMessages } from "react-intl";

export const scope = "sample.containers.App";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is Header",
  },
});
